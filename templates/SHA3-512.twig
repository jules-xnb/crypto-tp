{% extends 'template.twig' %}
{% block form %}
    <div class="form-group">
        <label for="sha3512">Votre texte en clair</label>
        {% if plain is defined %}
            <input type="text" class="form-control" id="sha3512" name="sha3512" value="{{ plain }}">
        {% else %}
            <input type="text" class="form-control" id="sha3512" name="sha3512">
        {% endif %}
    </div>
    <div class="form-group">
        {% if hash is defined %}
            Résultat du hash : {{ hash }}
        {% endif %}
    </div>
    <button type="submit" class="btn btn-primary">Hasher en SHA3-512</button>
{% endblock %}