{% extends 'template.twig' %}
{% block form %}
    <div class="form-group">
        <label for="keccak">Votre texte en clair</label>
        {% if plain is defined %}
            <input type="text" class="form-control" id="keccak" name="keccak" value="{{ plain }}">
        {% else %}
            <input type="text" class="form-control" id="keccak" name="keccak">
        {% endif %}
    </div>
    <div class="form-group">
        {% if hash is defined %}
            Résultat du hash : {{ hash }}
        {% endif %}
    </div>
    <button type="submit" class="btn btn-primary">Hasher en Keccak-512</button>
{% endblock %}