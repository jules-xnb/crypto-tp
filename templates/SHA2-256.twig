{% extends 'template.twig' %}
{% block form %}
    <div class="form-group">
        <label for="sha2">Votre texte en clair</label>
        {% if plain is defined %}
            <input type="text" class="form-control" id="sha2" name="sha2" value="{{ plain }}">
        {% else %}
            <input type="text" class="form-control" id="sha2" name="sha2">
        {% endif %}
    </div>
    <button type="submit" class="btn btn-primary">Hasher en sha2-256</button>
    <div class="form-group" style="margin-top: 20px;">
        {% if hash is defined %}
            <label for="textval">Résultat du hash :</label>
            <textarea class="form-control" id="my-textarea">{{ hash }}</textarea>
            <button type="button" onclick="download()" class="btn btn-primary">Sauvegarder</button>
        {% endif %}
    </div>
{% endblock %}